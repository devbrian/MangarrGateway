"""Manga Gateway package.

Single source of truth for the gateway version string. Used for the contract's
``version`` (GET /version) and ``gatewayVersion`` (GET /caps, Plan 02) fields.
"""

__version__ = "0.1.0"
